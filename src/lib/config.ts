import { dev } from '$app/environment'

export const title = 'Ideas and an accompanying journal'
export const description = 'SvelteKit blog for poets'
export const url = dev ? 'http://localhost:5173/' : 'https://joyofcode.xyz/'
